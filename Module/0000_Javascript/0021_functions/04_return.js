function tenTimes (number){
    const reuslt = number * 10;
    return reuslt;
}

function cutHalf (number){
    const half = number / 2;
    return half;
}

tenTimes(5);
const reuslt = tenTimes(5);
console.log('output', output);